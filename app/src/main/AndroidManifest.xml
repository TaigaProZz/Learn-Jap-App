<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.taigaprozz">

    <application
        android:allowBackup="true"
        android:banner="@drawable/wallpaper_main"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <activity android:name=".kanjikana.Kana.KanaAdapter" />
        <activity android:name=".kanjikana.Kana.KatakanaActivity" />
        <activity
            android:name=".kanjikana.Kana.HiraganaActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".kanjikana.Quiz.Quiz" />
        <activity android:name=".kanjikana.Kanji.KanjiActivity" />
        <activity android:name=".kanjikana.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>